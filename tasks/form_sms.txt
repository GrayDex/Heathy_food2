создать инфоблок "отклик на вакансию"
    название элементов: номер телефона, символьный код обяз
1) пользователь вводит данные и нажимает отправить:
    js:
        срабатывает событие change на форме:
            - собираем данные с формы и формируем json, включая значение из формы подтверждения кода даже если пустое(formCode)
            - отправляем ajax запрос в php файл
    php:
        проверка что это Ajax and Post
        декодирование json и получение полей формы
        валидация полей
         -> если не прошел вернуть ошибку

         -> иначе проверить существует ли элемент:
                CIBlockElement::GetList(arFilter: ['=CODE' => jsobData['mobile']]);
                если получен один, значит существует
                    -> если активный - вернуть что заявка уже существует
                    -> иначе нужно просто верифицировать:
                        если jsonData['formCode'] == '':
                            отправить смс, записать значение $refCode, 
                            перезаписать данные пользователя
                            refCode = $refCode
                        иначе проверить код из json['formCode'] == refCode:
                            если совпадает изменить атрибут в инфоблоке:
                                'GLOBAL_ACTIVE' = 'Y'
                                вернуть успех пользователю
                            иначе вернуть ошибку что код неправильный
                иначе отправить смс, записать код в $refCode переменную
                    создать инфоблок с данными пользователя:
                    - поля пользовательских данных
                    - поле инфоблока refCode = $refCode
                    - 'GLOBAL_ACTIVE' => 'N'
