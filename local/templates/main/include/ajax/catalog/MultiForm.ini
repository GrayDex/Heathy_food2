/form/
/form/#token#/1/
/form/#token#/2/
/form/#token#/3/
/form/#token#/4/

скрытое поле step в форме

получить элемент с токеном
    есть
        прочитать свойство статус

        статус=1-3 и step из формы меньше или равен
            валидация данных и запись
            status++
            редирект на /form/#token#/status/

        статус=4 и step из формы меньше или равен
            валидация данных и запись
            status='success'
            редирект на /form/#token#/success/

        статус='success'
            редирект на /form/#token#/success/
    нет
        сгенерировать строковый токен,
        создать новый элемент
            записать статус=1
            записать токен
            редирект на первую форму /form/#token#/1/
    